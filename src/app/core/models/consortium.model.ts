export interface Consortium {
  id: string;
  title: string;
  description: string;
  minValue: number;
  maxValue: number;
  minTerm: number;
  maxTerm: number;
  image: string;
  benefits: string[];
}